# Base URL of the Flask API. Copy this file to .env.local and adjust if
# your backend isn't running on the default port (e.g. macOS AirPlay
# Receiver commonly squats on port 5000 — run Flask on another port and
# point this at it instead).
VITE_API_BASE_URL=http://127.0.0.1:5000
